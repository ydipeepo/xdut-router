class_name RouteAnimationBase extends Resource

#-------------------------------------------------------------------------------
#	METHODS
#-------------------------------------------------------------------------------

func animate_enter(
	route_node: Node,
	route_cancel: Cancel) -> void:

	#
	# 継承先で実装します。
	#

	assert(false)

func animate_exit(
	route_node: Node,
	route_cancel: Cancel) -> void:

	#
	# 継承先で実装します。
	#

	assert(false)
