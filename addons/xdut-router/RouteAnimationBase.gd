class_name RouteAnimationBase extends Resource

#-------------------------------------------------------------------------------
#	METHODS
#-------------------------------------------------------------------------------

func animate_enter(route: Node) -> void:
	#
	# 継承先で実装します。
	#

	assert(false)

func animate_exit(route: Node) -> void:
	#
	# 継承先で実装します。
	#

	assert(false)
